import { Layers } from "lucide-react";
import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function ClassesPage() {
  return (
    <PagePlaceholder
      title="الصفوف والشعب"
      description="إدارة الصفوف الدراسية وشعبها."
      icon={Layers}
    />
  );
}
